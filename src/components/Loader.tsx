import { CircularProgress } from "@mui/material";
import React from "react";

const Loader = () => {
  return <CircularProgress className="dark:!text-[#ddd] !text-[#aa0022]" />;
};

export default Loader;
